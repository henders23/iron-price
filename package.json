{
  "name": "iron-price",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build-static.mjs",
    "preview": "vite preview",
    "check": "node --check src/game.js && node --check src/art-runtime.js && node --check src/music-player.js",
    "test": "npm run check && node scripts/verify-static.mjs && node scripts/test-music-player.mjs && node scripts/smoke-server.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "vite": "^7.0.0"
  }
}
